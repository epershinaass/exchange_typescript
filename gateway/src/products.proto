syntax = "proto3";

package products;

service ProductsController {
  rpc AllProducts (Object) returns (ArrayObjects);
}

message Object {
  string id = 1;
  string name = 2;
  uint64 count = 3;
}
message ArrayObjects {
  repeated Object total = 1;
}