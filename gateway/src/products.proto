syntax = "proto3";

package products;

service ProductsController {
  rpc AllProducts (ProductObject) returns (ArrayObjects);
}

message ProductObject {
  string productId = 1;
  string productName = 2;
  uint64 productCount = 3;
}
message ArrayObjects {
 repeated ProductObject total = 1;
}