syntax = "proto3";

package products;

service ProductsController {
  rpc AddProduct (AddProductRequest) returns (Product);
}

message Product {
  string productName = 1;
  uint64 productCount = 2;
}

message AddProductRequest {
  string userProductsId = 1;
  Product product = 2;
}
