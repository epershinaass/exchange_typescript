syntax = "proto3";

package products;

service ProductsController {
  rpc AllProducts (ProductObject) returns (ProductObject);
}

message ProductObject {
  string productName = 1;
  uint64 productCount = 2;
}
message ArrayObjects {
 repeated ProductObject total = 1;
}